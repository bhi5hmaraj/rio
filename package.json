{
  "name": "rio",
  "version": "0.1.0",
  "private": true,
  "description": "Rio - AI-powered conversation annotation for ChatGPT",
  "workspaces": [
    "extension"
  ],
  "scripts": {
    "dev": "npm run dev -w extension",
    "build": "npm run build -w extension",
    "test": "npm run test -w extension",
    "test:e2e": "npm run test:e2e -w extension",
    "lint": "npm run lint -w extension",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\""
  },
  "devDependencies": {
    "prettier": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
